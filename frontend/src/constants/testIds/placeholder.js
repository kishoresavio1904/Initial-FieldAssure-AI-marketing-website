// Test IDs for simple placeholder pages (Product, Use Cases, Pricing,
// Request Demo, About / Contact).

export const PLACEHOLDER = {
  page: (slug) => `placeholder-page-${slug}`,
  title: (slug) => `placeholder-title-${slug}`,
  description: (slug) => `placeholder-description-${slug}`,
  ctaPrimary: (slug) => `placeholder-cta-primary-${slug}-btn`,
  ctaSecondary: (slug) => `placeholder-cta-secondary-${slug}-btn`,
};
